#include < iostream>
using namespace std;
int main()
{
	cout << " pattern A \n";
	cout << endl;
	
for (int i = 1; i <= 6; i++)
	{
		for (int j = 1 ; j <= i ; j++)                  // j <= i       my error
		{
			cout << j <<" "  ;
		}
		cout << endl;
	}

	system("pause");
	return 0;
}
